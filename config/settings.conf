# 0 = 仅在 KernelSU Manager 中手动点击 Action 按钮时执行。
# 1 = 开机完成后自动对已启用的 App 重新补锁。
AUTO_RUN_ON_BOOT=0

# 修改 App 私有数据目录前，是否先强制停止该 App。
FORCE_STOP_APPS=1

# 本次运行有新锁定的目录时，是否一并清理 app_webview* 残留
# 以及 cache / code_cache 目录。
CLEAR_APP_CACHE=1
